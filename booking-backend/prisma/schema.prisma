generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id                        String   @id @default(uuid()) // UUID giống PrimaryGeneratedColumn('uuid')
  email                     String   @unique
  name                      String
  passwordHash              String
  role                      String   @default("user")
  currentHashedRefreshToken String?  // Nullable
  
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt
}